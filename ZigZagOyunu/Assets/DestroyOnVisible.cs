using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroyOnVisible : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        /* Vector3 difference = GameObject.FindGameObjectWithTag("Player").transform.position - transform.position;

         float ekranDikey = Camera.main.orthographicSize;

         if(difference.z > ekranDikey+2 )
         {
            Destroy(gameObject);               // Ekranın Gerisinde KALAN KÜPLERİ SİLME
         }
    */ } 
}
